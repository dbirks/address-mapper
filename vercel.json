{
  "buildCommand": "npm i -g pnpm && cd ui && pnpm config set registry https://registry.npmjs.org/ && pnpm install --no-frozen-lockfile && pnpm exec vite build && cd ../api && uv pip install -e .",
  "outputDirectory": "ui/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/main.py" }
  ],
  "functions": {
    "api/*.py": {
      "runtime": "@vercel/python@3.1.0"
    }
  }
}